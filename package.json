{
  "name": "beattek-events",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "@google/genai": "^1.27.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "esbuild": "^0.20.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0"
  },
  "scripts": {
    "start": "esbuild index.tsx --bundle --outdir=dist --servedir=dist --entry-names=[dir]/[name] --loader:.tsx=tsx",
    "build": "esbuild index.tsx --bundle --outdir=dist --minify --entry-names=[dir]/[name] --loader:.tsx=tsx",
    "type-check": "tsc --noEmit"
  }
}